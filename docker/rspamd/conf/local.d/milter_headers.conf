# Milter headers configuration
# Controls which headers Rspamd adds to messages

use = ["x-spam-status", "x-spam-score", "x-spam-symbol", "authentication-results"];

authenticated_headers = ["authentication-results"];

# Custom spam headers
extended_spam_headers = true;
