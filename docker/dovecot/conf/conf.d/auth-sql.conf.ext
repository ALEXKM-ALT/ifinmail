# Authentication via SQL
# Dovecot queries the ifinmail PostgreSQL database

passdb {
  driver = sql
  args = /etc/dovecot/conf.d/dovecot-sql.conf.ext
}

userdb {
  driver = sql
  args = /etc/dovecot/conf.d/dovecot-sql.conf.ext
}
