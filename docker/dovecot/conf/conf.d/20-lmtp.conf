# LMTP delivery configuration

protocol lmtp {
  mail_plugins = $mail_plugins sieve quota
  postmaster_address = postmaster@ifinmail.local
  lmtp_save_to_detail_mailbox = yes
  lmtp_rcpt_check_quit = no
  lmtp_proxy = no
}
