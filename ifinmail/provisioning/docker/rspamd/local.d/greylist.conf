# Rspamd greylisting — temp-fail unknown sender/recipient/IP tuples
# Backed by Redis for persistence across restarts
enabled = true;
expire = 1d;
timeout = 5min;
key_prefix = "rgrey:";
# Honor whitelists before greylisting
check_whitelist = true;
