# ifinmail Nginx fail2ban filter
# Matches repeated 401/403 responses on auth/admin endpoints

[Definition]
failregex = ^<HOST> .* "(POST|GET) /(manage-panel|admin)/.*" (401|403) .*$
            ^<HOST> .* "(POST|GET) /(manage-panel|admin)/login/.*" (401|403) .*$

ignoreregex =
