# ifinmail Dovecot fail2ban filter
# Matches IMAP/POP3 authentication failures

[Definition]
failregex = ^.*dovecot: auth.*: passdb\(\S+,<HOST>\).*Authentication failed.*$
            ^.*dovecot: auth-worker\(\d+\): sql\(\S+,<HOST>\).*Password mismatch.*$

ignoreregex =
